str = "true"
check = str.title()

print(check == True)

'''"True" → string
True → boolean
A string is never equal to a boolean → so result is False.'''


# .title() does not apply on booleans (True, False).
